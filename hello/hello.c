#include <stdio.h>
int main()
{
    int num1= 10,qube,result;
    Sum(qube);
    qube = num1*num1*num1+sum;

    printf("value %d",qube);

    return qube;
}

int Sum(qube)
{
    int a=10,b=15,sum;
    sum = a+b;
    //printf("value %d",sum);
    return sum;
}
